SHADOWBOX SESSION CONTINUITY — 2026-05-07 (updated)
==========================================

WHAT HAPPENED THIS SESSION (after the morning restart):

1. Brought services back to expected post-OpenClaw state
   - Booted out the orphaned ai.openclaw.gateway LaunchAgent (port 18789)
   - Loaded com.punchcyber.hook.web LaunchAgent (port 7799)
   - Verified ollama still running on 11434

2. Fixed scripts/restart.sh
   - Was crashing under set -e because grep for the now-removed gateway
     label returned nothing. Status subcommand now uses awk-based exact
     match, drops the gateway block, adds watch-check status.

3. Rewrote build/deploy artifacts to drop the gateway tier
   Commit 3b25bd7 — Drop OpenClaw gateway from build, deploy, and docs:
   - Deleted Dockerfile.gateway and deploy/openclaw-azure.json
   - docker-compose.yml: removed gateway service, HOOK_GATEWAY_URL, the
     openclaw-data volume; added an investigation-data mount on web
   - deploy/azure-setup.sh: dropped the gateway containerapp step
     (now [7/7] not [8/8]) and the HOOK_GATEWAY_URL env var
   - .github/workflows/deploy.yml: dropped GATEWAY_IMAGE, the gateway
     build step, and the gateway deploy step
   - docs/DEPLOY-AZURE.md: rewrote architecture diagram (2 container
     apps), dropped the "Configure OpenClaw" step, updated cost table
     to ~$165/mo (was ~$200/mo)
   - docs/BUILD-GUIDE.md: dropped gateway from container table, fixed
     build/ps output, updated provisioning step count
   - azure_prompt.md: rewrote for single-container App Service; added
     Ollama-vs-OpenAI-embeddings decision point

4. Removed dead OpenClaw code from server and Settings UI
   Commit 6787af2 — Remove dead OpenClaw remnants from server and Settings UI:
   - Deleted web/api/gateway_bridge.py (540 lines, no importers)
   - web/api/server.py: removed /api/config endpoint, CONFIG_TEMPLATE,
     _mask_secrets, SECRET_RE, unused `re` import, the openclaw.json
     fallback in _key_exists; rewrote the misleading "static fallback
     when gateway is unreachable" comment over the AGENTS list
   - web/src/pages/SettingsPage.jsx: replaced the dead "OpenClaw Gateway"
     tile (it read checks.gateway which never resolved) with an Agent
     Provider tile reading checks.agent_provider; dropped the now-broken
     Configuration panel and its /api/config fetch
   - web/src/pages/InvestigatePage.jsx: stale comment

5. Pushed both commits to origin/main (b5cd183..6787af2)

6. Rewrote install/INSTALL.md and install/setup.sh
   Commit aa7a987 — Rewrite install guide and setup script for post-OpenClaw stack:
   - INSTALL.md (615 -> 285 lines): new architecture diagram (browser ->
     FastAPI -> in-process agent runner -> OpenAI), prereqs trimmed to
     Python 3.13+/Node 22+/Homebrew/Ollama, manual-setup section for hand
     installs, troubleshooting covers web start failures, API key reload,
     Ollama unreachable, DB reset, and per-source enrichment debugging.
   - setup.sh (rewrite): idempotent. Checks prereqs, offers brew install
     for missing CLI tools, builds Python venv + frontend bundle, copies
     .env.example -> .env if missing and only prompts for blank keys
     (re-runs don't re-prompt for already-set keys), starts Ollama and
     offers to pull nomic-embed-text (default Y) and qwen2.5:14b
     (default N, 9GB), offers to install the LaunchAgent from the
     template, finishes with curl against /api/status.
   - Pushed: 124b307..aa7a987

7. Deleted dead OpenClaw-era scripts
   Commit 8c05b46 — Delete dead OpenClaw-era scripts and config template:
   - Removed scripts/health-check.sh, scripts/validate-config.sh,
     scripts/fix-channel-refs.sh, and config/openclaw.json.template
     (net -816 lines). All four were built around ~/.openclaw/openclaw.json
     and the Slack channel placeholder; /api/health on the FastAPI
     service now covers what health-check.sh did.
   - Dropped the matching `COPY config/openclaw.json.template` from
     Dockerfile.
   - Verified no live callers in scripts, plists, Docker, CI, or install
     guide. Historical mentions in docs/PHASE5-CHECKLIST.md and the old
     PROJECT-SYSTEM-PROMPT.md were left alone (handled in commit 8 below).

8. Refreshed README.md and PROJECT-SYSTEM-PROMPT.md for Phase 7
   Commit f7a27f9 — Refresh README and project system prompt for Phase 7:
   - README: architecture diagram now shows browser -> FastAPI -> agent
     runner -> OpenAI; Quick Start collapsed to ./install/setup.sh; OpenClaw
     Gateway dropped from the LaunchAgent table; npm/openclaw out of
     Requirements; gateway_bridge.py replaced with agent_runner.py in the
     file reference; pipelines/ flagged as "legacy Lobster YAMLs (no
     longer executed)"; Phase 7 row added.
   - PROJECT-SYSTEM-PROMPT.md: Phase 7 current state, Slack reframed as
     reserved-but-unwired, pipelines/*.yaml flagged as legacy without an
     executor, OpenClaw schema-quirk lessons replaced with the
     BLOCKED_PATTERNS / runner-guardrail notes, .env replaces
     ~/.openclaw/openclaw.json as the canonical config, repo tree
     refreshed, "do NOT bootstrap ai.openclaw.gateway" warning added.
   - Pushed: 8c05b46..f7a27f9

9. Deleted legacy Lobster pipelines
   Commit 3bda47c — Delete legacy Lobster pipelines:
   - Removed pipelines/{ioc-enrich-ip,ioc-enrich-domain,alert-to-report,
     batch-ioc-check}.yaml and docs/PIPELINES.md.
   - Removed the entire "Lobster Pipelines" section (47 lines) from
     workspaces/coordinator/TOOLS.md so Marshall stops suggesting
     lobster() tool calls that no longer exist.
   - Dropped /api/skills from web/api/server.py and its test.
   - Cleaned up two stale test_web_api.py items broken since 6787af2:
     TestStatusEndpoint asserted on the old "gateway" field (now
     agent_provider), and TestConfigEndpoint tested the deleted
     /api/config endpoint.

10. Fixed remaining test_web_api.py failures
    Commit 2e39aae — Fix SPA catch-all swallowing /api/* and stale conversations test:
    - SPA catch-all in web/api/server.py now returns 404 for paths
      starting with api/, matching the comment that already said "for
      all non-API routes". Previously, requests like
      /api/investigations/..%2Fetc%2Fpasswd were normalized by Starlette
      into a path that didn't match any /api/* route and fell through
      to the SPA catch-all returning index.html with 200 OK.
    - Replaced TestConversationsEndpoint.test_conversations_returns_list
      with test_conversations_requires_auth — the endpoint started
      requiring auth and the test never caught up. New assertion
      verifies unauthenticated callers get 401 (the actual security
      property), instead of pretending the endpoint is open.
    - Test suite is now green: 36/36 across test_web_api, test_mocks, test_rag.

CURRENT STATE:
- main is in sync with origin (commit 2e39aae)
- Mac Studio Shadowbox web running on port 7799
- Ollama running on 11434
- ai.openclaw.gateway LaunchAgent booted out (the plist still exists in
  ~/Library/LaunchAgents but is not loaded — leave it alone, it'll come
  back on next reboot if anyone runs `launchctl bootstrap` on it)
- Frontend rebuilt locally (web/dist), but dist is gitignored — CI/Docker
  rebuilds it from source

SERVICES RUNNING:
- com.punchcyber.hook.web (port 7799) — Shadowbox web UI + agent runner
- ollama (brew service, port 11434) — local embeddings + qwen2.5:14b
- ai.openclaw.hook-daily — daily feed fetch (6 AM)
- com.punchcyber.hook.watch-check — watchlist re-enrichment (every 4h)

AFTER REBOOT:
1. Shadowbox web should auto-start (LaunchAgent KeepAlive)
2. Ollama should auto-start (brew service)
3. Verify: ./scripts/restart.sh status
4. Test: curl http://localhost:7799/api/status
5. If web doesn't start: ./scripts/restart.sh web
6. If ai.openclaw.gateway comes back loaded after reboot, bootout again:
   launchctl bootout gui/$(id -u)/ai.openclaw.gateway
   (or just delete ~/Library/LaunchAgents/ai.openclaw.gateway.plist)

NEXT PRIORITIES:

1. Deploy to Azure App Service (azure_prompt.md is now accurate)
   - Web App: ShadowBox (rg-shadowbox)
   - Decision pending: Ollama as separate Web App vs OpenAI embeddings
     (single Web App, no VNet integration needed)
   - Database: Azure PostgreSQL
   - Reference: docs/DEPLOY-AZURE.md (Container Apps), azure_prompt.md
     (App Service adaptation)

2. Larger code-cleanup items left untouched (low priority):
   - web/api/server.py keeps its own AGENTS list (with role descriptions)
     duplicating the AGENTS dict in agent_runner.py (with model+callsign).
     Different shapes, both live, but worth unifying eventually.
   - docs/HOOK-Install-Guide.docx is a Word doc copy of the OLD install
     guide. Either regenerate from the new install/INSTALL.md or delete.
   - docs/PHASE5-CHECKLIST.md is a frozen Phase 5 status doc. Leave or
     archive — historical only.
   - config/ai.openclaw.hook-{daily,briefing}.plist still use the
     ai.openclaw.* label as cosmetic naming. The labels are live in
     launchctl. Renaming would be a larger surgery (rename plist file,
     bootout/boot-in, update restart.sh's DAILY_LABEL).

3. Remaining features (lower priority):
   - Export investigation as markdown/PDF
   - IOC quick-submit (paste list, batch enrich)
   - GreyNoise integration (#5 threat intel source)
   - Slack integration

API KEYS NEEDED (in .env or Azure Key Vault):
- OPENAI_API_KEY
- VT_API_KEY
- CENSYS_API_ID + CENSYS_API_SECRET
- ABUSEIPDB_API_KEY
- OTX_API_KEY
- SHODAN_API_KEY

FILE REFERENCE:
- web/api/agent_runner.py — in-process OpenAI agent runner
- web/api/server.py — FastAPI app; AGENTS list lives here for /api/agents
- Dockerfile — single-stage Node frontend + Python runtime, web only
- docker-compose.yml — local dev stack: web + db + ollama
- deploy/azure-setup.sh — provisions web + ollama Container Apps
- .github/workflows/deploy.yml — builds and deploys web image only
- azure_prompt.md — App Service deployment prompt for Claude Code session
- docs/BUILD-GUIDE.md, docs/DEPLOY-AZURE.md — operational guides
