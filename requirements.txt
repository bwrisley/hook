# HOOK Phase 6 Python dependencies
# Web UI
fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.27.0
pydantic>=2.0

# RAG / Vector Store
faiss-cpu>=1.7.4
numpy>=1.24.0

# Database (optional -- only needed with HOOK_OPENSEARCH_HOST)
opensearch-py>=2.4.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
